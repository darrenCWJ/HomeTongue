# admin/ is a standalone app, deliberately NOT part of the root pnpm workspace.
# NOTE: pnpm 10 ignores this setting during workspace discovery when it is set
# in a nested .npmrc, so installs MUST use the explicit CLI flag:
#   pnpm install --ignore-workspace
ignore-workspace=true
