# Training deps for train_sft.py / train_dpo.py (loosely pinned).
# prepare_sft_data.py / prepare_dpo_data.py are stdlib-only and need none of these.
torch>=2.1
transformers>=4.44,<5
trl>=0.12
peft>=0.11
datasets>=2.19
accelerate>=0.30
pyyaml>=6.0
# Optional, for 4-bit QLoRA on smaller GPUs (Linux/CUDA only):
# bitsandbytes>=0.43
